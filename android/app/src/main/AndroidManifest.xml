<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="preferExternal">

    <!-- ══════════════════════════════════════════════════════════════════
         DYLANDOS IPTV ULTIMATE - AndroidManifest
         ═══════════════════════════════════════════════════════════════ -->
         
    <uses-sdk tools:overrideLibrary="androidx.compose.animation, androidx.compose.animation.core, androidx.compose.material.ripple, androidx.compose.foundation, androidx.compose.foundation.layout, androidx.compose.material, androidx.compose.ui, androidx.compose.ui.graphics, androidx.compose.ui.text, androidx.compose.ui.util, androidx.compose.ui.tooling, androidx.compose.ui.tooling.preview, androidx.compose.ui.geometry, androidx.compose.ui.unit, androidx.compose.runtime, androidx.compose.runtime.saveable, androidx.compose.material3, androidx.compose.runtime.annotation, androidx.compose.ui.graphics.vector, androidx.compose.material.icons.core, androidx.compose.material.icons.extended, androidx.compose.ui.text.android, androidx.compose.runtime.retain, androidx.compose.foundation.text, androidx.window.core, androidx.window" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Legacy storage for Android 9 and below -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Sideloaded / Amazon Appstore builds — NOT for Play Store. Allows writing OTG paths on Android 11+ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- OTA updates: install downloaded APK -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- TV Features -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.type.television"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <!-- USB Host (OTG) — required for USB storage detection on FireOS -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- B5: backups disabled — Xtream credentials live in DataStore and must not
         leak into Google/ADB backup streams. This is a sideloaded Fire TV app. -->
    <application
        android:name=".DylandosApp"
        android:allowBackup="false"
        android:banner="@drawable/app_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DylandosIPTV"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|layoutDirection|keyboard|keyboardHidden"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.DylandosIPTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recording Service -->
        <service
            android:name=".service.RecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaPlayback" />

        <!-- FileProvider — used by OTA update to share downloaded APK with the installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Explicitly disable automatic WorkManager initialization.
             DylandosApp initializes WorkManager manually with custom config. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

        <!--
            Sentry is opt-in (BuildConfig.SENTRY_DSN is empty for this release).
            Its SDK still contributes ContentProviders which Android starts before
            DylandosApp.  On Fire OS that adds native-library initialization to
            the system splash window, even though telemetry is disabled.  Remove
            those providers until crash reporting is explicitly enabled for a
            tested distribution build.
        -->
        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.android.core.SentryPerformanceProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.ndk.SentryNdkPreloadProvider"
            tools:node="remove" />
    </application>
</manifest>
